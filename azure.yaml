# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: FullMcpApp
metadata:
  template: FullMcpApp@1.0.0
services:
  auth:
    project: ./auth/
    language: python
    host: function
    resourceGroup: ${AZURE_PREFIX}-${AZURE_ENV_NAME}auth-${AZURE_SUFFIX}
  app:
    project: ./app/
    language: python
    host: function
    resourceGroup: ${AZURE_PREFIX}-${AZURE_ENV_NAME}app-${AZURE_SUFFIX}
