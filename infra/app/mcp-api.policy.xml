<!--
    MCP API POLICY WITH EASY AUTH
    This policy applies to all operations in the MCP API.
    It uses the OAuth on-behalf-of flow to exchange the user's token for a Function App token.
-->
<policies>
    <inbound>
        <base />
        <check-header name="Authorization" failed-check-httpcode="401" failed-check-error-message="Not authorized" ignore-case="false" />

        <!--
            check cache with the Authorization header
            if cache miss
                hit auth server exchange endpoint with Authorization header + x-mcp-identifier header
                if response 200, cache response
            set authorization header
            set x-functions-key (from named value)
        -->

        <!-- Get the user's Entra token from cache using the session key -->
        <cache-lookup-value key="@($"EntraToken-{context.Request.Headers.GetValueOrDefault("Authorization")}")" variable-name="cachedToken" />
        
        <choose>
            <when condition="@(context.Variables.GetValueOrDefault("cachedToken") == null)">
                <!-- No cached token, perform token exchange -->
                <authentication-managed-identity resource="{{AuthServerIdentifier}}" output-token-variable-name="appToken" />
                <!-- Perform On Behalf Of (OBO) token exchange -->
                <send-request mode="new" response-variable-name="oboTokenResponse" timeout="120" ignore-error="false">
                    <set-url>{{AuthServerUrl}}/exchange</set-url>
                    <set-method>GET</set-method>
                    <set-header name="Authorization" exists-action="override">
                        <value>@(context.Request.Headers.GetValueOrDefault("Authorization"))</value>
                    </set-header>
                    <set-header name="x-mcp-identifier" exists-action="override">
                        <value>{{FunctionAppIdentifier}}</value>
                    </set-header>
                </send-request>

                <!-- Extract and cache the OBO token -->
                <set-variable name="oboTokenResponse" value="@{
                    var response = (IResponse)context.Variables["oboTokenResponse"];
                    if (response.StatusCode == 200) {
                        return response.Body.As<string>();
                    }
                    return null;
                }" />

                <!-- Check if OBO token was obtained successfully -->
                <choose>
                    <when condition="@(context.Variables.GetValueOrDefault<string>("oboTokenResponse") == null)">
                        <return-response>
                            <set-status code="401" reason="Unauthorized" />
                            <set-header name="Content-Type" exists-action="override">
                                <value>application/json</value>
                            </set-header>
                            <set-body>@{
                                var errorResponse = new JObject();
                                errorResponse["error"] = "token_exchange_failed";
                                errorResponse["error_description"] = "Unable to obtain token for Function App access";
                                return errorResponse.ToString();
                            }</set-body>
                        </return-response>
                    </when>
                </choose>

                <!-- Cache the OBO token response (expires in 50 minutes, similar to Entra token) -->
                <cache-store-value key="@($"EntraToken-{context.Request.Headers.GetValueOrDefault("Authorization")}")" value="@(context.Variables.GetValueOrDefault<string>("oboTokenResponse"))" duration="3600" />
                
                <!-- Extract the access token from the response -->
                <set-variable name="oboToken" value="@{
                    string responseJson = context.Variables.GetValueOrDefault<string>("oboTokenResponse");
                    JObject response = JObject.Parse(responseJson);
                    return response["access_token"]?.ToString();
                }" />
            </when>
            <otherwise>
                <!-- Use cached OBO token -->
                <set-variable name="oboToken" value="@{
                    string cachedResponseJson = context.Variables.GetValueOrDefault<string>("cachedToken");
                    JObject cachedResponse = JObject.Parse(cachedResponseJson);
                    return cachedResponse["access_token"]?.ToString();
                }" />
            </otherwise>
        </choose>

        <!-- Set the OBO token as the Authorization header -->
        <set-header name="Authorization" exists-action="override">
            <value>@("Bearer " + context.Variables.GetValueOrDefault<string>("oboToken"))</value>
        </set-header>
        
        <!-- Set the Function App key header -->
        <set-header name="x-functions-key" exists-action="override">
            <value>{{FunctionMcpKey}}</value>
        </set-header>
    </inbound>
    <backend>
        <base />
    </backend>
    <outbound>
        <base />
    </outbound>
    <on-error>
        <base />
    </on-error>
</policies>
